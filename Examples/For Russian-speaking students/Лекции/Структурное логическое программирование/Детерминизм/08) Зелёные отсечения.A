-------------------------------------------
--                                       --
-- Что такое зелёное отсечение?          --
--                                       --
-------------------------------------------

PREDICATES:

determ:

p(INTEGER)	- (i);

CLAUSES:

goal:-
	X== 9,
	p(X).

p(X):-
	X < 10,!,
	write("X < 10").
p(X):-
	X >= 10,
	write("X >= 10").
