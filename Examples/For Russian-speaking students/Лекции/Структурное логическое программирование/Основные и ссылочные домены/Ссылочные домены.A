-------------------------------------------
--                                       --
-- „то такое ссылочные домены            --
-- (типы данных)?                        --
--                                       --
-------------------------------------------

DOMAINS:

reference:

D1	= f(N,N,N).
N	= INTEGER.

PREDICATES:

nondeterm:

p(D1)		- (o);

CLAUSES:

goal:-
	p(X),
	writeln(X).

p(_).
p(f(3,_,5)).
