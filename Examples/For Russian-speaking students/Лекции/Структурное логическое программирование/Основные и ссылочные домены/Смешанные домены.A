-------------------------------------------
--                                       --
-- „то такое смешанные домены            --
-- (типы данных)?                        --
--                                       --
-------------------------------------------

DOMAINS:

mixed:

D1	= f(N,N,N).

reference:

N	= INTEGER.

PREDICATES:

nondeterm:

p(D1)		- (o);

CLAUSES:

goal:-
	p(X),
	writeln(X).

-- p(_).
p(f(3,_,5)).
