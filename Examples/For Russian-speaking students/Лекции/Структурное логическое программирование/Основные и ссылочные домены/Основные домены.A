-------------------------------------------
--                                       --
-- „то такое основные домены             --
-- (типы данных)?                        --
--                                       --
-------------------------------------------

DOMAINS:

ground:

D1	= f(N,N,N).
N	= INTEGER.

PREDICATES:

nondeterm:

p(D1)		- (o);

CLAUSES:

goal:-
	p(X),
	writeln(X).

-- p(_).
-- p(f(3,_,5)).
p(f(1,2,3)).
