-------------------------------------------
-- Элементы структурного логического     --
-- программирования.                     --
-- Что такое выходной аргумент           --
-- (выходной поток данных)?              --
-------------------------------------------

PREDICATES:

p(INTEGER)	- (o);

CLAUSES:

goal:-
	-- Аргумент X подпрограммы p
	-- является выходным.
	p(X),
	writeln(X).

p(Y):-
	Y == 100.
