-------------------------------------------
-- Элементы структурного логического     --
-- программирования.                     --
-- Что такое входной аргумент            --
-- (входной поток данных)?               --
-------------------------------------------

PREDICATES:

p(INTEGER)	- (i);

CLAUSES:

goal:-
	X == 100,
	-- Аргумент X подпрограммы p
	-- является входным.
	p(X).

p(Y):-
	writeln(Y).
